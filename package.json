{
  "name": "tradeco-norman-lp",
  "version": "1.0.0",
  "description": "Landing page de Influencer Boost (Tradeco Capital / Norman). Funnel Ads → LP → Telegram, sin formulario. Lógica + estilos servidos vía jsDelivr; Hostinger/Elementor solo aportan un punto de montaje.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "dev": "node esbuild.config.mjs --serve",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "gsap": "^3.13.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4"
  }
}
